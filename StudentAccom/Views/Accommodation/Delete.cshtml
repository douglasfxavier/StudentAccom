@model StudentAccom.Models.Accommodation

@{
    ViewBag.Title = "Delete Accommodation Advertisement";
    int id = Model.ID;
}

<h2>@ViewBag.Title</h2>
<h3>Are you sure you want to delete this Accommodation advertisement? </h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

    </dl>
    @using (Html.BeginForm("Delete","Accommodation", FormMethod.Post)) {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" />
        </div>
    }
</div>